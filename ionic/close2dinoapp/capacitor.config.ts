import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'close2dinoapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
